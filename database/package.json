{
  "name": "tgg-database",
  "version": "1.0.0",
  "description": "TGG Centralized Candle Database — PostgreSQL + TimescaleDB",
  "main": "src/index.js",
  "scripts": {
    "migrate": "node src/migrate.js",
    "test": "node src/healthcheck.js",
    "backfill:derivatives": "node src/backfillDerivatives.js",
    "test:symbolParser": "node src/symbolParser.test.js",
    "test:integration": "node src/integration.test.js",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}